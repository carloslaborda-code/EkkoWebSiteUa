const defaultUserSettings = {
  colorFilter: 'default',
  highContrast: false,
  textSize: 'medium',
  reducedMotion: false,
  largeTargets: false,
  underlineLinks: false,
  readableFont: false
};

const defaultUploads = [];

module.exports = {
  defaultUserSettings,
  defaultUploads
};
